name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - run: npm run build

      - name: Publish static export to gh-pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          rm -rf /tmp/henry-learning-os-gh-pages
          if git ls-remote --exit-code --heads origin gh-pages; then
            git fetch origin gh-pages
            git worktree add /tmp/henry-learning-os-gh-pages origin/gh-pages
          else
            git worktree add --detach /tmp/henry-learning-os-gh-pages
            cd /tmp/henry-learning-os-gh-pages
            git checkout --orphan gh-pages
            cd -
          fi

          find /tmp/henry-learning-os-gh-pages -mindepth 1 -maxdepth 1 \
            ! -name '.git' \
            ! -name '.github' \
            -exec rm -rf {} +
          cp -R out/. /tmp/henry-learning-os-gh-pages/
          touch /tmp/henry-learning-os-gh-pages/.nojekyll

          cd /tmp/henry-learning-os-gh-pages
          git add -A
          git commit -m "Deploy ${GITHUB_SHA}" || echo "No changes to commit"
          git push origin HEAD:gh-pages
          cd -
          git worktree remove /tmp/henry-learning-os-gh-pages --force
